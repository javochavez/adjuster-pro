// stub — nav pendiente de migración desde inline legacy
export const navPublic = {};
export function switchMain(a,b){ return window.switchMain ? window.switchMain(a,b) : undefined; }
export function switchTab2(a,b){ return window.switchTab2 ? window.switchTab2(a,b) : undefined; }
export function switchCat(a,b){ return window.switchCat ? window.switchCat(a,b) : undefined; }
export function irAExpediente(id){ return window.irAExpediente ? window.irAExpediente(id) : undefined; }
export function volverLista(){ return window.volverLista ? window.volverLista() : undefined; }
