// stub — render functions delegadas al inline legacy
export const renderPublic = {};
export function renderAjustadores(){ return window.renderAjustadores ? window.renderAjustadores() : undefined; }
export function renderOficinas(){ return window.renderOficinas ? window.renderOficinas() : undefined; }
export function renderSiniestros(){ return window.renderSiniestros ? window.renderSiniestros() : undefined; }
export function renderPolizas(){ return window.renderPolizas ? window.renderPolizas() : undefined; }
export function renderAseguradoras(){ return window.renderAseguradoras ? window.renderAseguradoras() : undefined; }
export function renderAsegurados(){ return window.renderAsegurados ? window.renderAsegurados() : undefined; }
export function renderContactos(){ return window.renderContactos ? window.renderContactos() : undefined; }
export function renderTC(){ return window.renderTC ? window.renderTC() : undefined; }
export function renderDash(){ return window.renderDash ? window.renderDash() : undefined; }
export function renderAlertas(){ return window.renderAlertas ? window.renderAlertas() : undefined; }
export function poblarSelectores(){ return window.poblarSelectores ? window.poblarSelectores() : undefined; }
export function renderDetalle(){ return window.renderDetalle ? window.renderDetalle() : undefined; }
