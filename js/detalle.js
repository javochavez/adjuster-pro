// stub — detalle pendiente de migración desde inline legacy
export const detallePublic = {};
export function renderDetalle(){ return window.renderDetalle ? window.renderDetalle() : undefined; }
export function verSiniestro(id){ return window.verSiniestro ? window.verSiniestro(id) : undefined; }
export function editarSiniestro(id){ return window.editarSiniestro ? window.editarSiniestro(id) : undefined; }
export function cerrarDetalle(){ return window.cerrarDetalle ? window.cerrarDetalle() : undefined; }
export function renderAjustBadges(id){ return window.renderAjustBadges ? window.renderAjustBadges(id) : undefined; }
export function renderTodoGrid(a,b){ return window.renderTodoGrid ? window.renderTodoGrid(a,b) : undefined; }
export function renderGrafGallery(id){ return window.renderGrafGallery ? window.renderGrafGallery(id) : undefined; }
export function buildCoberturaTableHTML(a,b){ return window.buildCoberturaTableHTML ? window.buildCoberturaTableHTML(a,b) : undefined; }
export function switchTab2(a,b){ return window.switchTab2 ? window.switchTab2(a,b) : undefined; }
export function cambiarEstatus(v){ return window.cambiarEstatus ? window.cambiarEstatus(v) : undefined; }
export function volverLista(){ return window.volverLista ? window.volverLista() : undefined; }
